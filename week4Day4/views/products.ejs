<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User List</title>
  </head>
  <body>
    <%- include('header') %>
    <div>
      <h1>Products List</h1>
      <ul>
        <% products.forEach(product => { %>
        <li><%= product.name %> costs <%= product.price %> years old</li>
        <% }) %>
      </ul>
    </div>
    <%- include('footer') %>
  </body>
</html>
